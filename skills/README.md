# Standalone Skills

Each child directory is a self-contained Codex skill. Copy the directory you need into `~/.codex/skills/` or the cross-runtime `~/.agents/skills/` directory.

每个子目录都是可独立安装的 Codex skill。将需要的目录复制到 `~/.codex/skills/` 或跨运行时目录 `~/.agents/skills/`。
